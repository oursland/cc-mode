class Frog
{
private:

    int a 
	(			// this parend should line up with 'int a'
	    int arg1,		// Can't see any reason for the comment
	    int arg2		// above, so I ignore it. :\  /mast
	    );

    int b (
        int arg1,
        int arg2
        )

        };
